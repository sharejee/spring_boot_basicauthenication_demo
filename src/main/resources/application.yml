spring:
  datasource:
    password:
    url: jdbc:mysql://localhost:3306/demo_real_2
    username: root
  jpa:
      generate-ddl: true
      hibernate:
        ddl-auto: create-drop
      database-platform: org.hibernate.dialect.MySQLDialect
show-sql: true